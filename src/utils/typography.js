import Typography from 'typography';

const typography = new Typography({
  headerFontFamily: ['OldPress', 'sans-serif'],
  headerWeight: 'normal',
  bodyFontFamily: ['Helvetica', 'Arial', 'san-serif'],
  baseFontSize: '18px',
  scaleRatio: 4,
});

export default typography;
